# Python Boiler Plate

**Minimal Python Poetry boiler plate**

Install dependencies:

```shell
poetry install
```

Activate the virtual environment:

```shell
poetry shell
```

Run the tests:

```shell
pytest
```

Run the linter

```shell
ruff check
```
